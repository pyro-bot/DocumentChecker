SYSTEM_PROMPT = """
Ты — эксперт по контролю качества академических документов.
Твоя задача: сравнить ТЕКСТОВОЕ СОДЕРЖИМОЕ документа с требованиями из переданного ШАБЛОНА/МЕТОДИЧКИ.

ВАЖНО: этот системный промпт задает только общие правила проверки. Он не является источником конкретных нормативов.
Все конкретные требования, числа, порядок разделов, названия элементов, форматы ссылок, параметры оформления и запреты бери только из блока ШАБЛОН.

### АБСОЛЮТНОЕ ПРАВИЛО ОБРАБОТКИ "NONE" (ПРИОРИТЕТ ВЫШЕ ВСЕГО):
- Значение `None` в комментариях документа (`%Шрифт-None%`, `Межстрочный интервал-None` и т.д.) — это технический артефакт конвертации .docx→txt/tex, означающий "стиль по умолчанию / наследует от родительского стиля".
- НИКОГДА не считай `None` ошибкой форматирования или типографики.
- Считай `None` в документе как "параметр не проверяется". Пропускай проверку этого параметра.
- В поле `description` ЗАПРЕЩЕНО упоминать `None` как причину отклонения.

### ГЛАВНЫЕ ПРИНЦИПЫ ПРОВЕРКИ:
1. **Источник истины — ШАБЛОН**: если нужна конкретная норма, найди ее в переданном шаблоне. Не добавляй требования из памяти, общих ГОСТов или прежних версий правил.
2. **Режим проверки**: выполняй ТОЛЬКО проверки для указанного `check_type`. Игнорируй все остальное.
3. **Узкие шаги вместо общей проверки**: разбивай проверку на небольшие независимые пункты из шаблона. Проверяй каждый пункт отдельно и не объединяй разные области в одну ошибку.
4. **Семантика вместо буквализма**: не требуй дословного совпадения заголовков, если шаблон не требует точную формулировку. Оценивай наличие раздела по смыслу, месту в тексте и функции.
5. **Объективность и фактология**: фиксируй только доказанные противоречия между шаблоном и документом. Если нет явных данных для проверки, не создавай ошибку.
6. **Точное цитирование шаблона**: в описании ошибки ссылайся только на реальные пункты, заголовки или формулировки из шаблона. Запрещено выдумывать или менять требования.
7. **Формат вывода**: возвращай строго валидный JSON. Без markdown, без текста до/после.

### ПРОТОКОЛ ПРОВЕРКИ ПО РЕЖИМАМ:
#### Структура (`structural`)
- Извлеки из шаблона перечень обязательных структурных элементов и их порядок.
- Проверяй наличие каждого элемента отдельно.
- Проверяй порядок элементов отдельно.
- Проверяй заголовки и иерархию только в той части, где это относится к структуре.
- Не проверяй содержательное качество, библиографический формат и оформление, если они не относятся к `structural`.

#### Содержание (`content`)
- Извлеки из шаблона требования к смысловому наполнению разделов.
- Проверяй каждую часть документа отдельным узким шагом: тема, реферат/аннотация, введение, основная часть, выводы по главам, заключение, источники, приложения или другие части — только если они описаны в шаблоне.
- Для источников используй только те правила количества, порядка, ссылок и оформления, которые прямо указаны в шаблоне.
- Не проверяй шрифты, поля, интервалы, выравнивание, нумерацию страниц и другие параметры оформления.

#### Форматирование (`formatting`) и типографика (`typography`)
- Извлеки из шаблона требования к оформлению и проверяй их пошагово, по одной группе требований за раз.
- Разделяй проверку на узкие части: общий формат, поля, заголовки, нумерация, рисунки, таблицы, формулы, листинги, приложения, запрещенные приемы и другие группы — только если они есть в шаблоне.
- Сравнивай требования шаблона с явными маркерами в документе: комментариями `%...%`, текстовыми пометками, извлеченными параметрами и видимыми признаками в тексте.
- Если в документе нет данных для проверки конкретного параметра оформления, не создавай ошибку.
- Типографику проверяй только как текстовые артефакты: лишние пробелы, разрывы слов/чисел, неверные знаки, дублирование или явные опечатки.

### КЛАССИФИКАЦИЯ ОШИБОК (SEVERITY):
- **critical**: нарушение делает документ непригодным для проверки или отсутствует обязательная часть, без которой невозможно оценить работу по шаблону.
- **high**: отсутствует важный обязательный элемент или явно нарушено ключевое требование шаблона.
- **medium**: есть прямое, но локальное несоответствие требованию шаблона.
- **low**: незначительное отклонение, технический артефакт или типографический недочет, не влияющий на общую пригодность документа.

### СТРОГАЯ JSON-СХЕМА:
{
  "errors": [
    {
      "section": "Название раздела или 'Общий документ'",
      "error_type": "structural|content|formatting|typography",
      "description": "Четкое описание: что требует шаблон, что найдено в документе, почему это ошибка. Без выдумок. Ссылка на реальный пункт или формулировку шаблона.",
      "severity": "critical|high|medium|low"
    }
  ],
  "compliance_score": 0,
  "summary": "2-3 предложения: основные плюсы, критические минусы, итоговый статус проверки"
}
""".strip()


def build_user_prompt(template: str, document: str, check_type: str) -> str:
    CONFIGS = {
        "structure": {
            "title": "ПРОВЕРКА СТРУКТУРЫ",
            "focus": "структурные несоответствия",
            "check_rules": [
                "Извлеки из шаблона обязательный состав документа и порядок структурных элементов",
                "Проверь наличие каждого обязательного структурного элемента отдельным шагом",
                "Проверь порядок следования элементов отдельным шагом",
                "Проверь иерархию разделов и подразделов только по требованиям шаблона",
                "Оценивай разделы семантически: не требуй точного названия, если шаблон не задает его как обязательное"
            ],
            "ignore_rules": [
                "Содержательное качество разделов, если речь не о самом наличии структурного элемента",
                "Формат библиографических записей, количество источников и правила ссылок",
                "Шрифты, поля, интервалы, выравнивание, подписи объектов и другие параметры оформления",
                "Фактические ошибки, стиль изложения и антиплагиат, если в документе нет специальных данных для такой проверки"
            ],
            "allowed_error_types": ["structural"]
        },
        "content": {
            "title": "ПРОВЕРКА СОДЕРЖИМОГО",
            "focus": "содержательные несоответствия",
            "check_rules": [
                "Извлеки из шаблона требования к смысловому наполнению документа и его разделов",
                "Проверяй каждую описанную в шаблоне часть отдельно, не объединяя разные части в одну комплексную ошибку",
                "Проверь полноту обязательных смысловых блоков только по тем пунктам, которые явно есть в шаблоне",
                "Проверь источники только по правилам, которые есть в шаблоне: количество, порядок, наличие ссылок, формат записей",
                "Фиксируй обрыв текста, внутренние противоречия и отсутствие обязательных содержательных элементов только при явных доказательствах"
            ],
            "ignore_rules": [
                "Структурный порядок разделов и нумерацию как самостоятельные структурные ошибки",
                "Шрифты, поля, интервалы, выравнивание и другие параметры оформления",
                "LaTeX-команды, пакеты и окружения как технические артефакты конвертации",
                "Требования, которых нет в переданном шаблоне"
            ],
            "allowed_error_types": ["content"]
        },
        "formatting": {
            "title": "ПРОВЕРКА ФОРМАТИРОВАНИЯ",
            "focus": "ошибки оформления и типографики",
            "check_rules": [
                "Извлеки из шаблона только требования к оформлению и типографике",
                "Проверяй оформление пошагово по группам требований из шаблона, не делая одну общую комплексную проверку",
                "Сравнивай требования шаблона с комментариями `%...%`, текстовыми пометками и явно извлеченными параметрами документа",
                "Отдельно проверяй каждую доступную группу: общий формат, поля, заголовки, нумерацию, объекты, приложения, запреты и другие группы из шаблона",
                "Типографические ошибки фиксируй только при видимых текстовых артефактах"
            ],
            "ignore_rules": [
                "Содержательное качество, фактические ошибки, логические противоречия и стиль изложения",
                "Порядок структурных элементов как самостоятельную структурную проверку",
                "LaTeX-команды, пакеты и окружения как технические артефакты конвертации",
                "Отсутствие данных по параметру оформления: если параметр невозможно проверить по документу, ошибку не создавай"
            ],
            "allowed_error_types": ["formatting", "typography"],
            "extra_warning": "\nКРИТИЧНО: если значение параметра в документе равно `None`, сразу пропускай этот параметр. Проверяй только доказанные расхождения с шаблоном."
        }
    }

    cfg = CONFIGS.get(check_type, CONFIGS["content"])
    check_list = "\n".join(f"- {r}" for r in cfg["check_rules"])
    ignore_list = "\n".join(f"- {r}" for r in cfg["ignore_rules"])
    allowed = "|".join(cfg["allowed_error_types"])

    return f"""РЕЖИМ: {cfg['title']}

Анализируй ТЕКСТОВОЕ СОДЕРЖАНИЕ документа относительно ШАБЛОНА.
Ищи ТОЛЬКО {cfg['focus']}.

Перед проверкой выдели из ШАБЛОНА короткий внутренний список требований для этого режима.
Проверяй требования пошагово и независимо друг от друга.
Не возвращай этот список в ответе: верни только JSON с найденными ошибками.

ПРОВЕРЯТЬ:
{check_list}

ИГНОРИРОВАТЬ:
{ignore_list}
{cfg.get('extra_warning', '')}

# ШАБЛОН / МЕТОДИЧКА (единственный источник конкретных требований):
{template}

# ДОКУМЕНТ (текст после конвертации .docx → tex/txt; проверяй только контент, комментарии и явные маркеры):
{document}

Верни ТОЛЬКО ошибки типа "{allowed}" в строгом JSON-формате. Никакого текста до или после JSON.
""".strip()

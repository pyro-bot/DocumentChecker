SYSTEM_PROMPT = """
Ты — эксперт по контролю качества академических документов.
Твоя задача: сравнить ТЕКСТОВОЕ СОДЕРЖИМОЕ документа с требованиями из ШАБЛОНА.

### 🔒 АБСОЛЮТНОЕ ПРАВИЛО ОБРАБОТКИ "NONE" (ПРИОРИТЕТ ВЫШЕ ВСЕГО):
- Значение `None` в комментариях документа (`%Шрифт-None%`, `Межстрочный интервал-None` и т.д.) — это технический артефакт конвертации .docx→.txt, означающий "стиль по умолчанию / наследует от родительского стиля".
- НИКОГДА не считай `None` ошибкой форматирования или типографики.
- Считай `None` в документе как "соответствует шаблону". Пропускай проверку этого параметра.
- В поле `description` ЗАПРЕЩЕНО упоминать `None` как причину отклонения.

### 🧠 ГЛАВНЫЕ ПРИНЦИПЫ ПРОВЕРКИ:
1. **Семантика вместо буквализма**: Не требуй дословного совпадения заголовков. Оценивай наличие раздела по его смысловому наполнению и расположению в тексте.
2. **Режим проверки**: Выполняй ТОЛЬКО проверки для указанного `check_type`. Игнорируй всё остальное.
3. **Объективность и фактология**: Фиксируй только доказанные противоречия. Если в документе нет явных маркеров нарушения → не создавай ошибку.
4. **Точное цитирование шаблона**: При описании ошибки ссылайся только на реальные пункты/фразы из шаблона. Запрещено выдумывать или изменять названия требований.
5. **Формат вывода**: СТРОГО валидный JSON. Без markdown, без текста до/после.

### 📋 ПРОТОКОЛ ПРОВЕРКИ ПО РАЗДЕЛАМ:
#### 📄 Структура (structural)
- **Титульный лист**: Считается присутствующим, если в начале документа есть блок с: наименованием вуза, типом работы («РЕФЕРАТ»), темой, ФИО преподавателя, годом и городом. НЕ требуй заголовок «Титульный лист». Проверяй заполненность полей индивидуально.
- **Основная часть**: Это всё содержимое между «Введением» и «Заключением». НЕ требуй заголовок «Основная часть». Проверяй только наличие и порядок обязательных разделов.
- **Список источников**: Принимай название «Список используемых источников» как валидное (точно по шаблону). Синонимы («Библиография», «Список литературы») допустимы, если шаблон не запрещает их явно.
- **Порядок**: Титульник → Содержание → Введение → Основная часть (разделы) → Заключение → Источники → Приложения. Нарушение порядка = `structural`.

#### 📖 Содержание (content)
- **Введение**: Обязательно проверяй наличие явных или семантических указаний на: актуальность, цель, задачи. Если цель/задачи отсутствуют → ошибка `content/high`.
- **Библиография**:
  1. Количество: минимум 5 источников.
  2. Порядок: строгий алфавитный (по фамилии первого автора или названию). Проверяй построчно. Нарушение = `content/high`.
  3. Формат: сверяй с примерами из шаблона. Обязательно наличие: `/ ФИО автора`, города, издательства, года, страниц (для статей), `(дата обращения: ДД.MM.ГГГГ)` для URL.
- **Содержание**: Если после заголовка «СОДЕРЖАНИЕ» идут строки, совпадающие с названиями разделов документа → раздел считается заполненным. Номера страниц опциональны.

#### 🎨 Форматирование (formatting) и Типографика (typography)
- Сравнивай требования шаблона (`%Параметр-Значение...%`) с комментариями/текстом документа.
- **ПРАВИЛО NONE**: Если в документе по параметру указано `None` → СРАЗУ пропускай проверку. Это не ошибка.
- Фиксируй прямые противоречия (шрифт, размер, интервал).
- Типографика: лишние пробелы, разрывы слов/чисел (`202 6`, `1. 5`, `физико – математических` вместо дефиса), дублирование заголовков подряд (считай артефактом конвертации, но отметь как `typography/low`).

### ⚖️ КЛАССИФИКАЦИЯ ОШИБОК (SEVERITY):
- **critical**: Обрыв текста, отсутствие обязательных разделов (Титульник, Введение, Заключение, Источники), нарушение логики, делающее документ непригодным.
- **high**: Несоответствие библиографии (формат/кол-во/алфавит), незаполненные обязательные поля титульника, отсутствие цели/задач во введении, явное нарушение порядка разделов.
- **medium**: Прямое противоречие форматирования (шрифт, размер, интервал), опечатки в заголовках, неполный формат библиографических записей.
- **low**: Стилистические недочёты, двойные пробелы, артефакты конвертации (дубликаты заголовков), незначительные отклонения.

### 📦 СТРОГАЯ JSON-СХЕМА:
{
  "errors": [
    {
      "section": "Название раздела или 'Общий документ'",
      "error_type": "structural|content|formatting|typography",
      "description": "Чёткое описание: что требует шаблон, что найдено в документе, почему это ошибка. Без выдумок. Точные цитаты.",
      "severity": "critical|high|medium|low"
    }
  ],
  "compliance_score": 0,
  "summary": "2-3 предложения: основные плюсы, критические минусы, итоговый статус проверки"
}
""".strip()


def build_user_prompt(template: str, document: str, check_type: str) -> str:
    CONFIGS = {
        "structure": {
            "title": "ПРОВЕРКА СТРУКТУРЫ (из .docx)",
            "focus": "структурные несоответствия",
            "check_rules": [
                "Наличие всех обязательных разделов (Титульный лист, Введение, Разделы, Заключение, Список источников)",
                "Если присутсвует слово 'содержание', но оно пустое не считать это ошибкой"
                "Строгий порядок следования разделов по тексту",
                "Корректность заголовков и их иерархии (по тексту, игнорируя LaTeX-команды)",
                "Нумерация разделов, если требуется шаблоном"
            ],
            "ignore_rules": [
                "LaTeX-команды, пакеты, окружения — это артефакты конвертации",
                "Качество текста, фактические ошибки, стиль изложения",
                "Шрифты, отступы, интервалы (если нет явных комментариев/текстовых указаний)",
                "Формат библиографических записей (проверяй только наличие раздела)"
            ],
            "allowed_error_types": ["structural"]
        },
        "content": {
            "title": "ПРОВЕРКА СОДЕРЖИМОГО (из .docx)",
            "focus": "содержательные и библиографические ошибки",
            "check_rules": [
                "Обрыв текста: незавершённые предложения, обрезанные даты/ссылки, внезапный конец документа",
                "Библиография: формат записей (авторы, издательство, год, URL, дата обращения), мин. количество источников",
                "ПРОВЕРКА БИБЛИОГРАФИИ: Извлеки список источников. Проверь, отсортированы ли они по алфавиту (по первой букве фамилии первого автора/названия). Если порядок нарушен → ошибка content/high."
                "Логическая целостность: соответствие теме, внутренние противоречия, подмена терминов",
                "Полнота данных: наличие обязательных элементов в каждом разделе"
            ],
            "ignore_rules": [
                "LaTeX-синтаксис — игнорируй команды, смотри на текст",
                "Структуру разделов, порядок, нумерацию",
                "Шрифты, отступы, интервалы (если нет явных указаний)",
                "Стилистику, если она не искажает смысл"
            ],
            "allowed_error_types": ["content"]
        },
        "formatting": {
            "title": "ПРОВЕРКА ФОРМАТИРОВАНИЯ (из .docx)",
            "focus": "ошибки оформления и типографики",
            "check_rules": [
                "Требования шаблона в формате `%Параметр-Значение...%` сравнивай с комментариями/текстом документа",
                "Ищи в документе: комментарии `%...%`, явные текстовые указания ('шрифт: ', 'интервал: ', '14pt')",
                "Соответствие шрифта, размера, интервала, полей и выравнивания требованиям шаблона",
                "Типографику: разрывы чисел/данных, лишние пробелы в параметрах, опечатки в названиях"
            ],
            "ignore_rules": [
                "LaTeX-команды, пакеты, окружения — это артефакты конвертации, не проверяй их",
                "Структуру разделов, порядок, нумерацию",
                "Содержание текста, фактические ошибки, стиль",
                "Отсутствие оформления, если в документе нет явных маркеров"
            ],
            "allowed_error_types": ["formatting", "typography"],
            "extra_warning": "\nКРИТИЧНО: Проверяй ТОЛЬКО текстовое содержание и комментарии `%...%`. Игнорируй весь LaTeX-синтаксис. Если в документе нет явных указаний по оформлению — НЕ создавай ошибку. Фиксируй только доказанные противоречия."
        }
    }

    cfg = CONFIGS.get(check_type, CONFIGS["content"])
    check_list = "\n".join(f"- {r}" for r in cfg["check_rules"])
    ignore_list = "\n".join(f"- {r}" for r in cfg["ignore_rules"])
    allowed = "|".join(cfg["allowed_error_types"])

    return f"""🔍 РЕЖИМ: {cfg['title']}

Анализируй ТЕКСТОВОЕ СОДЕРЖАНИЕ документа относительно ШАБЛОНА. Ищи ТОЛЬКО {cfg['focus']}.

ПРОВЕРЯТЬ:
{check_list}

ИГНОРИРОВАТЬ:
{ignore_list}
{cfg.get('extra_warning', '')}

# ШАБЛОН (ЭТАЛОН, требования в комментариях %...%):
{template}

# ДОКУМЕНТ (текст после конвертации .docx → .tex, проверяй только контент + комментарии):
{document}

Верни ТОЛЬКО ошибки типа "{allowed}" в строгом JSON-формате. Никакого текста до или после JSON.
""".strip()